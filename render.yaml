services:
  - type: web
    name: api-vagas-skills
    env: python
    buildCommand: pip install -r requirements-render-supabase.txt
    startCommand: uvicorn api_vagas_skills:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_KEY
        sync: false
      - key: PYTHON_VERSION
        value: 3.11.0